---
- name: setup wsl
  gather_facts: no
  hosts: localhost
  become: yes
  roles:
    - common
    - python
    - clojure
    - ruby
    - rust
  vars:
    ubuntu: no
    home_dir: /home/toracle

# clojure
# rust
# python3
# nodejs
# roswell
# emacs
# zsh
# omz
# mysql
# build-essential
