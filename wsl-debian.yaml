---
- name: setup wsl
  gather_facts: no
  hosts: localhost
  become: yes
  roles:
    - common
    - python
    - clojure
    - ruby
    - rust
    - nodejs
  vars:
    linux_dist: debian
    home_dir: /home/toracle

# clojure
# rust
# python3
# nodejs
# roswell
# emacs
# zsh
# omz
# mysql
# build-essential
